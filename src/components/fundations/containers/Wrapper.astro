---
interface Props {
  variant?: "standard" | "wide" | "prose"; // Define any variants you need
  class?: string; // Optional prop for additional classes
}
const { variant = "standard", class: extraClass = "" } = Astro.props;
// Map each variant to its specific classes
const variantClasses = {
  // All purpose wrapper
  standard: "max-w-2xl 2xl:max-w-3xl mx-auto px-8 w-full",
  // Wide wrapper for headers and hero sections
  wide: "max-w-5xl mx-auto px-8 w-full",
  // Wrapper for prose blocks
  prose: "prose max-w-2xl 2xl:max-w-3xl pt-0 mx-auto prose-a:text-black dark:prose-a:text-white prose-p:text-black dark:prose-p:text-white prose-code:text-black dark:prose-code:text-white prose-strong:text-black dark:prose-strong:text-white prose-ol:text-black dark:prose-ol:text-white prose-ul:text-black dark:prose-ul:text-white prose-headings:text-black dark:prose-headings:text-white prose-headings:text-base text-lg"
,
};
// Combine the classes for the specified variant with any extra classes
const classes = `${variantClasses[variant]} ${extraClass}`.trim();
---

<div class={classes}>
  <slot />
</div>
